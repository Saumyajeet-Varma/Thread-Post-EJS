<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login Page</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div
      class="w-full min-h-screen bg-zinc-900 p-10 text-white flex justify-center items-center"
    >
      <div class="w-full max-w-sm">
        <h2 class="text-3xl mb-5 text-center">Login Account</h2>
        <form action="/login" method="post">
          <!-- <label for="email" class="block text-sm mb-1">Email</label> -->
          <input
            id="email"
            class="w-full px-3 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md mb-4 focus:border-blue-500 focus:ring focus:ring-blue-500/50"
            type="email"
            placeholder="Email"
            name="email"
            required
            aria-label="Email Address"
          />
          <!-- <label for="password" class="block text-sm mb-1">Password</label> -->
          <input
            id="password"
            class="w-full px-3 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md mb-4 focus:border-blue-500 focus:ring focus:ring-blue-500/50"
            type="password"
            placeholder="Password"
            name="password"
            required
            aria-label="Password"
          />
          <input
            class="w-full px-3 py-2 bg-blue-700 rounded-md cursor-pointer hover:bg-blue-600 active:bg-blue-800 disabled:bg-gray-500 disabled:cursor-not-allowed"
            type="submit"
            value="Login Account"
          />
        </form>
        <div class="w-full flex justify-center mt-3">
          <a class="text-blue-700 inline-block" href="/">Create new account</a>
        </div>
      </div>
    </div>
  </body>
</html>
