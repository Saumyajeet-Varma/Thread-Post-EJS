<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create Account</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div
      class="w-full min-h-screen bg-zinc-900 p-10 text-white flex items-center justify-center"
    >
      <div class="w-full max-w-md">
        <h2 class="text-3xl mb-5 text-center">Create Account</h2>
        <form action="/register" method="post" class="space-y-4">
          <div>
            <!-- <label for="name" class="block mb-1 text-sm">Name</label> -->
            <input
              id="name"
              class="w-full px-3 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md focus:border-blue-500"
              type="text"
              placeholder="Name"
              name="name"
              required
            />
          </div>
          <div>
            <!-- <label for="username" class="block mb-1 text-sm">Username</label> -->
            <input
              id="username"
              class="w-full px-3 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md focus:border-blue-500"
              type="text"
              placeholder="Username"
              name="username"
              required
            />
          </div>
          <div>
            <!-- <label for="email" class="block mb-1 text-sm">Email</label> -->
            <input
              id="email"
              class="w-full px-3 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md focus:border-blue-500"
              type="email"
              placeholder="Email"
              name="email"
              required
            />
          </div>
          <div>
            <!-- <label for="age" class="block mb-1 text-sm">Age</label> -->
            <input
              id="age"
              class="w-full px-3 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md focus:border-blue-500"
              type="number"
              placeholder="Age"
              name="age"
              min="1"
              required
            />
          </div>
          <div>
            <!-- <label for="password" class="block mb-1 text-sm">Password</label> -->
            <input
              id="password"
              class="w-full px-3 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md focus:border-blue-500"
              type="password"
              placeholder="Password"
              name="password"
              minlength="6"
              required
            />
          </div>
          <div>
            <input
              class="w-full px-3 py-2 bg-blue-700 rounded-md cursor-pointer hover:bg-blue-600 active:bg-blue-800 disabled:bg-gray-500 disabled:cursor-not-allowed"
              type="submit"
              value="Create Account"
            />
          </div>
        </form>
        <div class="w-full flex justify-center mt-3">
          <a class="text-blue-700 inline-block" href="/login"
            >Login to existing account</a
          >
        </div>
      </div>
    </div>
  </body>
</html>
